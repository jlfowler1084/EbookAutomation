# EbookAutomation — Python 3.12 dependencies
# Install: py -3.12 -m pip install -r requirements.txt

# Core PDF extraction
pypdf==6.9.2
pdfminer.six==20251230
PyMuPDF==1.27.2.2
pdf2image==1.17.0
pytesseract==0.3.13

# EPUB/ebook support
EbookLib==0.20
beautifulsoup4==4.14.3

# AI/API
google-genai==1.68.0
openai==1.109.1
anthropic>=0.50.0
requests==2.33.0

# Text processing
pyspellchecker==0.9.0

# Optional — coordinate-based heading resolution (degrades gracefully if missing)
pdfplumber==0.11.9

# Optional — Kokoro neural TTS (SCRUM-325)
# Install via scripts\install-kokoro.ps1, which handles onnxruntime-gpu/cpu conflict.
# kokoro-onnx       # core synthesis library (installs own onnxruntime dep)
# soundfile         # WAV I/O for chapter files
# onnxruntime-gpu   # GPU inference (mutually exclusive with onnxruntime)
# onnxruntime       # CPU-only inference (use -CpuOnly flag in install script)

# Web service (EB-45)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
filetype>=1.2.0
aiofiles>=24.1.0
# Stripe billing + crypto (EB-45 Phase 2)
stripe>=12.0.0,<13.0.0
cryptography>=48.0.0,<49.0.0
# Send-to-Kindle (EB-324 Wave 1 Unit 4) — Resend SDK + RFC-5322 recipient parsing
resend>=2.30.0,<3.0.0
email-validator>=2.3.0,<3.0.0
# Resend webhook signature verification (EB-324 Wave 1 Unit 10) — Svix client
svix>=1.90.0,<2.0.0

# Config
python-dotenv==1.2.2

# Transitive / required by other packages
lxml==6.0.2                # Required by ebooklib, pdfminer
pillow==12.1.1             # Required by pdf2image, pytesseract
charset-normalizer==3.4.6  # Required by requests
