-r requirements.txt

# Testing
pytest==9.0.2
# httpx is the transport for fastapi.testclient.TestClient (FastAPI 0.100+).
# Usually pulled in transitively, but pinning here keeps a clean CI runner
# from failing to import TestClient on first install.
httpx>=0.27.0
# Used by some web-service tests that await async helpers directly.
pytest-asyncio>=0.24.0
